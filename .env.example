GEMINI_API_KEY=               # Google AI Studio API key
STORAGE_BACKEND=memory        # "memory" for local dev, "vercel" for production
KV_REST_API_URL=              # Upstash Redis URL (auto-set when linked in Vercel dashboard)
KV_REST_API_TOKEN=            # Upstash Redis token (auto-set when linked)
BLOB_READ_WRITE_TOKEN=        # Vercel Blob token (auto-set when linked)
NEXT_PUBLIC_BASE_URL=http://localhost:3000  # Change for production
